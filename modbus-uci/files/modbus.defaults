#!/bin/sh

. /lib/functions.sh

[ "$(uci get ucitrack.@modbus[0] 2>/dev/null)" != "modbus" ] && \
uci batch <<-EOF >/dev/null 2>/dev/null
	add ucitrack modbus
	set ucitrack.@modbus[-1].init="modbus"
	commit ucitrack
EOF
